import { Routes } from '@angular/router';
import { CustomerPageComponent } from './presentation/pages/customer-page.component';

export const CUSTOMER_ROUTES: Routes = [
  { path: '', component: CustomerPageComponent }
];